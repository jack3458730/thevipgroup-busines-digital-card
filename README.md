=========
Info
=========

Simple business card for TheViPGroup company, meant to be accessed after scanning a QR code or reading an NFC chip.
Uses CSS (and Bootstrap as a framework), JavaScript with Vue.js as well as HTML.
Heavy work in progress.
